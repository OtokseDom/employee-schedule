services:
    db:
        image: mysql:8
        container_name: mysql_db
        restart: unless-stopped
        environment:
            MYSQL_ROOT_PASSWORD: root
            MYSQL_DATABASE: v2
            MYSQL_USER: root
            MYSQL_PASSWORD: root
        volumes:
            - db_data:/var/lib/mysql

    backend:
        build: ./backend
        container_name: laravel_app
        restart: unless-stopped
        expose:
            - "80" # expose to other containers or reverse proxy
        environment:
            APP_NAME: Laravel
            APP_ENV: production
            APP_DEBUG: false
            APP_TIMEZONE: Asia/Dubai
            APP_URL: http://otokse_service.lidta.com
            DB_CONNECTION: mysql
            DB_HOST: db
            DB_PORT: 3306
            DB_DATABASE: v2
            DB_USERNAME: root
            DB_PASSWORD: root
            SESSION_DRIVER: database
            CACHE_STORE: database
            QUEUE_CONNECTION: database
        depends_on:
            - db
        volumes:
            - ./backend:/var/www/html

    frontend:
        build: ./frontend
        container_name: react_app
        restart: unless-stopped
        expose:
            - "3000"
        environment:
            REACT_APP_API_URL: http://otokse_service.lidta.com/api
        depends_on:
            - backend
        volumes:
            - ./frontend:/app

volumes:
    db_data:
